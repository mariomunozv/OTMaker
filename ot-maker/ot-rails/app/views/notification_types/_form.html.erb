<%= form_for(@notification_type) do |f| %>
<% if @notification_type.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@notification_type.errors.count, "error") %> prohibited this notification_type from being saved:</h2>

    <ul>
        <% @notification_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

<div class="form-group">
                <%= f.label :name %>
            <%= f.text_field :name, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :template %>
            <%= f.text_field :template, :class => "form-control" %>
    </div>
<div class="actions">
    <%= f.submit :class => "btn btn-success" %>
</div>
<% end %>
