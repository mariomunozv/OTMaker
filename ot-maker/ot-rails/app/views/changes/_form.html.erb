<%= form_for(@change) do |f| %>
<% if @change.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@change.errors.count, "error") %> prohibited this change from being saved:</h2>

    <ul>
        <% @change.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

<div class="form-group">
                <%= f.label :work_order_id %>
            <%= f.collection_select :work_order_id, WorkOrder.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="form-group">
                <%= f.label :user_id %>
            <%= f.collection_select :user_id, User.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="form-group">
                <%= f.label :code %>
            <%= f.text_field :code, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :title %>
            <%= f.text_field :title, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :description %>
            <%= f.text_field :description, :class => "form-control" %>
    </div>
<div class="form-group">
                <%= f.label :bill_order_id %>
            <%= f.collection_select :bill_order_id, BillOrder.all, :id, :name, prompt: true, :class => 'form-control'  %>
    </div>
<div class="actions">
    <%= f.submit :class => "btn btn-success" %>
</div>
<% end %>
